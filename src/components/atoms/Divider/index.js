import React from 'react';

/**
 * Display a horizontal divider line with 20px top & bottom margin
 * @return {React.Element} Return Divider component UI
 */
const Divider = () => (
  <hr style={{ margin: '20px 0' }} />
);

export default Divider;
